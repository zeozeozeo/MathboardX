# nanovg-mathboardx

NanoVG port taken from `vendor:nanovg` with some modifications for MathboardX (notably: stroke baking, crash safety and fixes for huge zoom + strokes, wasm support).

All the changes in the source code are documented as comments starting with `MATHBOARDX: `
